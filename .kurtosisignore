# Kurtosis uploads this directory on every run and does NOT honour .gitignore, so build
# output has to be excluded here too. `make bin` writes 140MB+ into bin/, which stalls
# `kurtosis run` at the upload step with no output and no error.
bin/
artifacts/
**/*.log
